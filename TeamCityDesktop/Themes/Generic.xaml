<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:Controls="clr-namespace:TeamCityDesktop.Controls">
	<ResourceDictionary.MergedDictionaries>
		<ResourceDictionary Source="pack://application:,,,/Themes/ServerViewDataTemplate.xaml" />
		<ResourceDictionary Source="pack://application:,,,/Themes/BuildViewModelDataTemplate.xaml" />
	</ResourceDictionary.MergedDictionaries>

	<Style TargetType="{x:Type TreeViewItem}">
		<Setter Property="IsExpanded"
		        Value="{Binding IsExpanded, Mode=TwoWay}" />
		<Setter Property="IsSelected"
		        Value="{Binding IsSelected, Mode=TwoWay}" />
	</Style>

	<Style x:Key="LoadingGif"
	       TargetType="Controls:GifImage">
		<Style.Setters>
			<Setter Property="GifSource"
			        Value="../Images/Loading.gif" />
			<Setter Property="Stretch"
			        Value="None" />
			<Setter Property="AutoStart"
			        Value="True" />
		</Style.Setters>
	</Style>

	<Style TargetType="{x:Type TreeView}">
		<Setter Property="ItemTemplate">
			<Setter.Value>
				<HierarchicalDataTemplate ItemsSource="{Binding Children}">
					<ContentPresenter Content="{Binding}" />
				</HierarchicalDataTemplate>
			</Setter.Value>
		</Setter>
	</Style>
</ResourceDictionary>