namespace TeamCityDesktop.ViewModel
{
    // a placeholder so that treeviews has children (so they can be expanded)
    internal class LoadingViewModel : TreeViewModelBase
    {
        protected override void Refresh()
        {
        }
    }
}
